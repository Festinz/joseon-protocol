Review this Three.js rail-shooter game code for correctness bugs. Focus files: src/combat.js, src/cover.js, src/enemies.js, src/weapons.js, src/flow.js, src/config.js. The core rule: a "danger shot" must ONLY hit the player if player state != COVERED at impact; cover snap-in timing (PEEK.snapInMs) is the handedness penalty; ammo reload = min(maxMag-current, reserve) subtracted from reserve. Hunt for: state machine edge cases (RISING/SNAPPING/TRANSIT interactions), timing bugs (wall-clock vs dt mixing), raycast layer/origin mistakes, ammo math errors, event listener leaks, pool reuse bugs. Report findings as a numbered list with file:line and a one-line fix each. Do NOT modify files.
